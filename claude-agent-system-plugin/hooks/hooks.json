{
  "description": "Surface open /orchestrate handoffs at session start; command approval is handled by the user-level rm/commit/push harness",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/orchestrate-resume.js"
          }
        ]
      }
    ]
  }
}
